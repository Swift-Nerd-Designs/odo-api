# ============================================================
# Client API — .env.example
# Copy to .env and fill in values. Never commit .env.
# ============================================================

CI_ENVIRONMENT = development

# App
app.baseURL         = http://localhost:8080/
app.allowedOrigins  = http://localhost:3000

# Database
database.default.hostname = localhost
database.default.database = client_db
database.default.username = root
database.default.password =
database.default.DBDriver = MySQLi
database.default.port      = 3306

# Contact form recipient
app.contactToEmail = consultation@odocorp.co.za

# Email via Resend (use contact.odocorp.co.za subdomain)
RESEND_API_KEY =
RESEND_FROM    = noreply@contact.odocorp.co.za

# Cloudinary (for server-side upload fallback — browser upload is preferred)
CLOUDINARY_CLOUD_NAME  =
CLOUDINARY_API_KEY     =
CLOUDINARY_API_SECRET  =
